# API Base URL (backend)
VITE_API_URL=http://localhost:8080/v1

# Frontend Port (development only)
VITE_PORT=8081

# Dev: bypass authentication (set to "true" for local dev without backend)
VITE_DEV_BYPASS_AUTH=false
